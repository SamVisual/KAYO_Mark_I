{
  "identifier": "default",
  "description": "KAYO default capability – grants core window + invoke + sidecar access",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "shell:allow-execute",
    "shell:allow-spawn",
    "shell:allow-kill"
  ]
}
